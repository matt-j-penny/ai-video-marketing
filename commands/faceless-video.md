---
description: Make a faceless video in CapCut from one topic — script, voiceover, AI images, Ken Burns, word-highlighted captions.
argument-hint: <topic> [step by step] [one-off overrides, e.g. "30s, 16:9"]
---

Run the `capcut-faceless-video` skill (`skills/capcut/faceless-video/SKILL.md`) on the request below.

0. Check requirements, then load the user's saved preferences (or run first-time setup if none exist).
1. Decide the run mode from the request: all at once by default, or step by step (stop after every stage, show its output, wait for "next") if the request asks for it.
2. Research → script → voiceover → transcribe → shot list → images → assemble → Ken Burns → captions → verify.

$ARGUMENTS
