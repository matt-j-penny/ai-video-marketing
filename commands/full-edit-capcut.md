---
description: Run the full CapCut pipeline end to end — rough cut, then plan and build motion graphics throughout, verified.
argument-hint: <source-video-path> [draft-name] [brand/style notes]
---

Run the `capcut-full-edit` skill end to end (`skills/capcut/full-edit/SKILL.md`) on the footage below:

0. Check requirements (ffmpeg, Deepgram and Brave keys), then load the user's saved preferences (or run first-time setup if none exist), per the skill's Requirements and Preferences sections.
1. Rough cut the raw source — strip silences and bad takes.
2. Plan beats across the whole edited timeline and build the motion graphics they call for (full-screen, overlay, or split layout, per beat), placed as CapCut overlay tracks. Sleek and professional, on-brand, fast-moving, never a dead stretch on screen.
3. Run the skill's verification loop before reporting anything finished — don't stop after just the rough cut, and don't skip the loop.

$ARGUMENTS
